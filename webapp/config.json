{
  "backendUrl": "",
  "_comment": "EMPTY = direct mode (browser talks to FSM directly). This is what you want for now. Only set a REAL Cloudflare Worker URL here later if creating activities returns 403/CORS. The placeholder example is ignored automatically."
}
