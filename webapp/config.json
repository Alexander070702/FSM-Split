{
  "backendUrl": "https://evosplit-proxy.YOUR-SUBDOMAIN.workers.dev",
  "_comment": "Set this to your Cloudflare Worker URL. Leave '' only if the UI is served from the same origin as the proxy. The UI calls backendUrl + '/fsm/...'."
}
